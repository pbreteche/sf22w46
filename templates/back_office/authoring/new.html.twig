{% extends 'back_office/back_office_layout.html.twig' %}

{% block body %}
    {{  form_start(create_form) }}
    {{  form_widget(create_form) }}
    <button class="btn btn-primary">Enregistrer</button>
    {{  form_end(create_form) }}
{% endblock %}
